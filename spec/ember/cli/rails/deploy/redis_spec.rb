require 'spec_helper'

describe Ember::Cli::Rails::Deploy::Redis do
  it 'has a version number' do
    expect(Ember::Cli::Rails::Deploy::Redis::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
